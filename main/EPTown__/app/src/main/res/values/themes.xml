<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EPTown_" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- 상태바 배경을 하얀색으로 설정 -->
        <item name="android:statusBarColor">@android:color/white</item>
        <!-- 상태바 아이콘을 검정색으로 설정 -->
        <item name="android:windowLightStatusBar">true</item>
        <!-- 네비게이션 바 배경색을 흰색으로 설정 -->
        <item name="android:navigationBarColor">@android:color/white</item>
        <!-- 폰트 설정 -->
        <item name="android:fontFamily">@font/notosanskr_medium</item>
        <!-- 글자 padding 제거 -->
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="Theme.EPTown_" parent="Base.Theme.EPTown_" />

    <!-- 스플래시 스크린 전용 테마 -->
    <style name="SplashScreenTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 스플래시 스크린 배경을 주황색으로 설정 -->
        <item name="android:windowBackground">@color/orange_splash</item>
    </style>
</resources>